{
  "name": "sqg-analyze-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only test harness for the Sales Quote Generator 'Analyze this page' feature. NOT part of the Chrome extension (not referenced by manifest.json).",
  "scripts": {
    "test": "node analyze.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
